name: PR Lint Tests

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  actions: read
  checks: write
  contents: write
  pull-requests: write

jobs:
  test:
    uses: ./.github/workflows/node-tests.yml
    secrets: inherit
  run-check:
    if: ${{ github.event_name != 'push' }}
    needs: test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - uses: ./
  success:
    needs: [test, run-check]
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Workflow successful"